---
name: algorithm
description: Algorithm v4.0 — ведение рискованной или крупной работы через PRD с проверяемыми критериями, доказательствами и независимой проверкой. Применять при destructive/irreversible операциях, production-инцидентах, миграциях и риске потери данных, security/auth/payments, широких cross-system или многофайловых изменениях, а также когда пользователь просит «algorithm».
---

# Algorithm v4.0

Algorithm переводит систему из текущего состояния в целевое через проверяемые **критерии** и **доказательства**. PRD — журнал прогона: что должно стать правдой, чем это доказано и чем прогон закончился.

Прогон ведёт главный агент, и только он пишет PRD. Subagent получает в задании свой срез критериев и возвращает доказательства; главный агент переносит их в PRD.

## Вывод пользователю

- При входе — одна строка: `Algorithm v4.0 · <effort> · <slug>`.
- По ходу — короткий прогресс только тогда, когда он несёт факт.
- В финале — результат, путь к PRD, итоговая `phase` и `verified_by`.

## Шаги

### 1. Observe

1. Прочитай последние 5 строк `~/.claude/MEMORY/LEARNING/REFLECTIONS/algorithm-reflections.jsonl` и выпиши уроки, которые относятся к задаче.
2. Разбери запрос: что явно просили, что явно исключили, что очевидно нежелательно, насколько срочно.
3. Выбери effort по таблице ниже.
4. Создай `~/.claude/MEMORY/WORK/<YYYYMMDD-HHMMSS>_<slug>/PRD.md` с frontmatter из раздела «Формат PRD», `phase: observe`.

Готово, когда PRD существует и в `## Context` записаны запрос, границы и не-цели.

### 2. Plan

1. Запиши критерии в `## Criteria`. Критерий — **наблюдаемый результат** в мире после работы: состояние файла, вывод команды, поведение UI, строка в БД, ответ API. Одно проверяемое утверждение на критерий, 8–14 слов.
2. Критерии проверяют систему, над которой идёт работа. Формат собственных артефактов (промпт субагенту, отчёт, сам PRD) — это ход работы, его в критерии не пишут.
3. Anti-criteria `ISC-A<n>` фиксируют то, что должно остаться нетронутым: данные, прод без явного OK, чужие изменения.
4. Риски и premortem — в `### Risks`; для advanced и deep технический план — в `### Plan`.
5. Обнови `phase: plan`, `progress: 0/N`.

Готово, когда каждое явное требование пользователя покрыто хотя бы одним критерием.

### 3. Execute

1. `phase: execute`. Работай инструментами OMP: `task` для независимых срезов, `skill://…` для профильных процедур, `lsp` и `ast_edit` для кода.
2. Отмечай критерий `[x]` в той же правке, где добавляешь его доказательство в `## Verification`, и сразу обновляй `progress`.
3. Нетривиальные решения и отклонения от плана — в `## Decisions`.

Готово, когда каждый критерий либо отмечен с доказательством, либо известна причина, по которой он недостижим.

### 4. Verify

1. `phase: verify`. Для каждого `[x]` в `## Verification` есть строка `ISC-<n>: <команда | файл | URL> → <наблюдённый результат>`. Доказательство — результат, полученный в этом прогоне.
2. **Attestation** обязательна для advanced и deep, а также для любого прогона с production-действиями или необратимыми операциями. Проверяет не автор:
   - subagent `reviewer` с путём к PRD и diff;
   - `xd://loop_validate`, если работа идёт в engineering-loop;
   - пользователь, если проверка требует его доступа.
3. Запиши во frontmatter `verified_by: reviewer:<id>`, `loop_validate` или `user`.

Готово, когда каждый отмеченный критерий подтверждён, а у прогонов, где нужна attestation, заполнено `verified_by`.

### 5. Close

Прогон заканчивается терминальной фазой в том же сеансе:

| phase | когда |
|---|---|
| `complete` | все критерии отмечены и подтверждены |
| `partial` | часть сделана, остаток осознанно отложен |
| `blocked` | нужен внешний вход: решение, доступ, квота |
| `abandoned` | пользователь сменил направление или задача потеряла смысл |

Для `partial`, `blocked` и `abandoned` заполни `## Outcome`: что сделано, что нет, что нужно для продолжения. Затем добавь рефлексию.

Готово, когда `phase` терминальная и строка рефлексии записана.

## Effort

| effort | когда | критериев | бюджет |
|---|---|---|---|
| `standard` | ограниченная задача, 1–3 файла или одна система | 4–12 | ~15 мин |
| `advanced` | многофайловая или многосистемная работа, продакшен | 10–30 | ~60 мин |
| `deep` | архитектура, миграции данных, несколько репозиториев | 20–60 | ~3 ч |

Число критериев — ориентир, а не норма: достаточно, чтобы каждое требование пользователя было покрыто.

## Формат PRD

```yaml
---
algorithm: "4.0"
task: <одна строка>
slug: <YYYYMMDD-HHMMSS_kebab-slug>
effort: standard | advanced | deep
phase: observe | plan | execute | verify | complete | partial | blocked | abandoned
progress: <отмечено>/<всего>
started: <ISO 8601>
updated: <ISO 8601>
verified_by: <reviewer:<id> | loop_validate | user>
---
```

Разделы по мере заполнения: `## Context` (с `### Risks` и `### Plan`), `## Criteria`, `## Decisions`, `## Verification`, `## Outcome`.

Критерии: `- [ ] ISC-1: …`; anti-criteria: `- [ ] ISC-A1: …`.

## Guard

Extension `algorithm-guard` (исходник `extension/algorithm-guard.ts` этого скилла, symlink в `~/.omp/agent/extensions/`) проверяет каждую запись PRD с `algorithm: "4.0"`:

- `progress` равен числу отмеченных критериев из общего числа;
- `complete` требует, чтобы все критерии были отмечены и каждый id встречался в `## Verification`;
- `complete` для advanced и deep требует `verified_by` от проверяющего, не от автора;
- `partial`, `blocked` и `abandoned` требуют непустой `## Outcome`.

Нарушение возвращается ошибкой инструмента: исправь PRD и повтори запись. Команда `/algorithm-doctor` показывает незакрытые прогоны, невалидные PRD и посторонние файлы в `MEMORY/WORK`.

## Рабочая папка

`~/.claude/MEMORY/WORK/` хранит только папки прогонов `<YYYYMMDD-HHMMSS>_<slug>/` с PRD и доказательствами. Бэкапы, выгрузки и копии конфигураций кладутся в `~/.claude/MEMORY/BACKUPS/`. Секреты в PRD и логах маскируются как `***`.

## Рефлексия

Добавь одну строку в `~/.claude/MEMORY/LEARNING/REFLECTIONS/algorithm-reflections.jsonl`:

```json
{"timestamp":"<ISO 8601>","algorithm":"4.0","effort_level":"<effort>","task_description":"<task>","prd_id":"<slug>","phase":"<терминальная phase>","criteria_count":0,"criteria_passed":0,"criteria_failed":0,"implied_sentiment":0,"within_budget":true,"reflection_q1":"<что стоило сделать иначе>","reflection_q2":"<как сработал бы более умный процесс>","reflection_q3":"<какой инструмент или скилл стоило задействовать>"}
```

Урок формулируй как конкретное действие для следующего прогона.

## Восстановление после compaction

Текущий прогон — самый свежий по mtime PRD в `~/.claude/MEMORY/WORK/` с нетерминальной фазой. Frontmatter даёт фазу и прогресс, `## Criteria` и `## Verification` — состояние работы.
